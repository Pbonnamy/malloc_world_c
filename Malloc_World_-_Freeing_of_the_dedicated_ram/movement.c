#include "header.h"

void handleMovement(Levels *lv){

    lv->columns = 14;

    /*char direction;

    do{
        printf("\nWhich direction ? : ");
        scanf("%c", &direction);
        fflush(stdin);
        printf("Direction : %c\n", direction);

    }while(direction != 'p');*/
}
