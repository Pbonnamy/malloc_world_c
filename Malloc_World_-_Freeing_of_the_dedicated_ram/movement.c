#include "header.h"

void handleMovement(Levels *lv){

    printMap(lv->lv1,lv->rows,lv->columns);

    /*char direction;

    do{
        printf("\nWhich direction ? : ");
        scanf("%c", &direction);
        fflush(stdin);
        printf("Direction : %c\n", direction);

    }while(direction != 'p');*/
}
